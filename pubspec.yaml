name: projetoeleitoral
description: "Sistema de Consulta Eleitoral"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fl_chart: ^0.69.0
  crypto: ^3.0.3
  intl: ^0.19.0
  uuid: ^4.4.0
  file_picker: ^8.0.0
  image_picker: ^1.1.0
  universal_io: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
  # fonts removed: use system defaults to avoid missing asset errors